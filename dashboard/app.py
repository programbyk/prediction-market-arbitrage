"""Dashboard placeholder.

The CLI scanner is functional. The dashboard will consume exported
opportunities in a later commit.
"""
def main() -> None:
    print("Dashboard module prepared; run `python main.py` for the scanner.")

if __name__ == "__main__":
    main()
